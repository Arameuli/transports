#include "Transport.h"
Transport :: Transport() {
}
Transport :: Transport(int wheel, int length, int width, int hight) {
	this->wheel = wheel;
	this->length = length;
	this->width = width;
	this->hight = hight;
}